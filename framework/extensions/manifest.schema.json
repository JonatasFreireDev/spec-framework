{
  "required": ["id", "version", "capabilities"],
  "capabilities": ["artifacts.read", "events.read", "reviews.import", "runtime.dispatch"],
  "authority": "declarative only; capability does not grant approval, commit, push, merge, or writes outside a separately enforced scope"
}
