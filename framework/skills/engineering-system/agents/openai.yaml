interface:
  display_name: "Engineering System"
  short_description: "Consolidate and validate the Engineering System"
  default_prompt: "Use $engineering-system to consolidate, version, validate, or migrate the Engineering System aggregate and Quality System after specialist contracts are ready."
