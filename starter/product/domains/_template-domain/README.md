# Domain Template

Copy this folder to a stable business-area slug after the applicable entry gate is satisfied.

- Start with `context.md` and `domain.md`.
- Define what the domain owns and explicitly does not own.
- Record dependencies before creating the first goal.
- Replace every `TBD`; never approve the template folder itself.

Next: copy `goals/_template-goal/` for one user outcome owned by this domain.
