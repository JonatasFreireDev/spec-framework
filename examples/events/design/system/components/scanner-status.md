# Component: Scanner Status

`COMP-002`, draft since `0.1.0`. States: loading, ready, validating, success, already-checked-in, expired, invalid, permission-denied, camera-denied, and network-error. Every state includes text, iconography, focus-safe recovery, and an appropriate live-region policy.
