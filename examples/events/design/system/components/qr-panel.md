# Component: QR Panel

`COMP-001`, draft since `0.1.0`. Presents a short-lived attendee QR with textual status, expiration, and refresh. It never exposes raw tokens or uses the QR image as the only accessible representation. States: loading, active, expired, error, and success. Actions are keyboard reachable with a 44px target.
