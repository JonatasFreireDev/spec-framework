# Draft Foundations

Semantic status colors always include text/iconography; typography uses a readable system fallback; spacing follows a four-pixel scale; motion is optional and reduced-motion aware; controls require visible focus, keyboard access, 44px touch targets, and sufficient contrast.
