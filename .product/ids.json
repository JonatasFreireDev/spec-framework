{
  "policy": "slug-scoped",
  "status": "approved",
  "decision": "DEC-009",
  "rules": {
    "slug": "Immutable folder slug declared in context.md at artifact creation.",
    "id_scope": "IDs are unique within the parent scope; include path when ambiguity is possible.",
    "allocation": "Do not allocate IDs by incrementing this file.",
    "move_tool": "node engineering/move-artifact.mjs --from <old-path> --to <new-path>"
  },
  "deprecated_counters": true
}
